import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: "'M PLUS Rounded 1c', 'Noto Sans JP','Rampart One', sans-serif",
  },
});

export default theme;
